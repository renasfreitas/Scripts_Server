#!/bin/bash

# Caminho que serah desmontado apos final do backup
MOUNT_POINT=/mnt/bkp

if ! mountpoint "$MOUNT_POINT" ; then
        echo "Compartilhamento nao montado!"
        exit 1
fi

#Desmonta compartilhamento

cd /
/bin/umount -l $MOUNT_POINT
exit 0
# end of "backuphd-multi.post"
